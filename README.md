# Zyndex-Frontend

Frontend for the Zyndex app.

## Deployment

Build command: `npm run build`

Publish directory: `dist`

Set `VITE_API_BASE_URL` to the deployed backend API URL, ending with `/api`.
